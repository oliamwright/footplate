<h1>Scheduler</h1>

<%= render 'info_links' %>

<h2>Scheduled posts</h2>

<% if @posts.any? %>
  <%= render partial: 'posts_table', locals: { posts: @posts } %>
<% else %>
  <h3>Nothing scheduled</h3>
<% end %>
