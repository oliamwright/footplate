%tr
	/%td= post.title_with_link
	%td
		.button= post.unpublish_link
	%td.ready
		= post.title
		%a.link
			= post.bitly_link
	/
		%td{:id => "feed-entry-#{post.id}-body"}
			= render partial: 'feed_entries/body', locals: { feed_entry: post }
	%td
		.highlight= post.sent_at
